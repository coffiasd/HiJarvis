{
    "createERC20TokenFunc": {
        "name": "createERC20Token",
        "description": "mint some ERC20 token",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "the name of ERC20 contract"
                },
                "symbol": {
                    "type": "string",
                    "description": "the symbol of ERC20 contract"
                }
            }
        }
    },
    "mintERC20TokenFunc": {
        "name": "mintERC20Token",
        "description": "mint some ERC20 token",
        "parameters": {
            "type": "object",
            "properties": {
                "contract_address": {
                    "type": "string",
                    "description": "the address of ERC20 contract"
                }
            }
        }
    },
    "checkNativeTokenBalanceFunc": {
        "name": "checkNativeTokenBalance",
        "description": "check curent wallet native token balance",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    "transferERC20TokenFunc": {
        "name": "transferERC20Token",
        "description": "transfer some ERC20 token",
        "parameters": {
            "type": "object",
            "properties": {
                "contract_address": {
                    "type": "string",
                    "description": "To address of ERC20 token contract"
                },
                "to_address": {
                    "type": "string",
                    "description": "To address for transfer token"
                },
                "amount": {
                    "type": "string",
                    "description": "amount of token to send to"
                }
            }
        }
    },
    "transferNativeTokenFunc": {
        "name": "transferNativeToken",
        "description": "transfer some native token",
        "parameters": {
            "type": "object",
            "properties": {
                "to_address": {
                    "type": "string",
                    "description": "To address for transfer token"
                },
                "amount": {
                    "type": "string",
                    "description": "amount of token to send to"
                }
            }
        }
    },
    "switchNetworkFunc": {
        "name": "switchNetwork",
        "description": "switch network",
        "parameters": {
            "type": "object",
            "properties": {
                "chainId": {
                    "type": "string",
                    "description": "To chainId of network"
                }
            }
        }
    },
    "createAbstractionAccountFunc": {
        "name": "createAbstractionAccount",
        "description": "create an abstraction account",
        "parameters": {
            "type": "object",
            "properties": {
                "addr1": {
                    "type": "string",
                    "description": "the first address of owners"
                },
                "addr2": {
                    "type": "string",
                    "description": "the second address of owners"
                },
                "addr3": {
                    "type": "string",
                    "description": "the third address of owners"
                }
            }
        }
    },
    "createAbstractionAccountTransactionFunc": {
        "name": "createAbstractionAccountTransaction",
        "description": "create abstraction account transaction",
        "parameters": {
            "type": "object",
            "properties": {
                "safeAddress": {
                    "type": "string",
                    "description": "the address of safe account transaction contract"
                },
                "to_address": {
                    "type": "string",
                    "description": "the address to receive token"
                },
                "amount": {
                    "type": "string",
                    "description": "the amount of token value"
                }
            }
        }
    },
    "signAbstractionAccountTransactionFunc": {
        "name": "signAbstractionAccountTransaction",
        "description": "sign an abstraction account transaction",
        "parameters": {
            "type": "object",
            "properties": {
                "safeAddress": {
                    "type": "string",
                    "description": "the address of safe account transaction contract"
                }
            }
        }
    },
    "executeAbstractionAccountTransactionFunc": {
        "name": "executeAbstractionAccountTransaction",
        "description": "execute an abstraction account transaction",
        "parameters": {
            "type": "object",
            "properties": {
                "safeAddress": {
                    "type": "string",
                    "description": "the address of safe account transaction contract"
                }
            }
        }
    }
}